# Music Streaming Analysis Using Spark Structured APIs

## Overview

This project implements a series of hands‑on data processing tasks using Apache Spark.
The goal is to analyze user listening behavior from a synthetic music‑streaming dataset, perform aggregations, join logs with metadata, and generate insights such as favorite genres, average listening time, and late‑night listening patterns.

The assignment demonstrates:

- Loading structured CSV data into Spark
- Defining schemas
- Joining datasets
- Using window functions
- Writing results to output directories

The project is designed to run in a reproducible environment (Docker recommended due to Windows filesystem issues with Spark 4.x).

## Dataset Description

The repository includes two synthetic datasets generated by `datagen.py`:

`listening_logs.csv`: Contains individual listening events:

- `user_id `— unique identifier for a user
- `song_id` — unique identifier for a song
- `timestamp `— when the listening event occurred
- `duration_sec` — number of seconds the user listened

`songs_metadata.csv`: Contains metadata for each song:

- `song_id `— unique identifier
- `title` — song title
- `artist` — performing artist
- `genre` — genre label
- `mood` — mood classification

These two datasets are joined on `song_id` for tasks requiring genre‑level analysis.

## Repository Structure

```
6190-handson-l6/
│
├── main.py                 # Spark job implementing assignment tasks
├── datagen.py              # Script that generates synthetic CSV datasets
├── listening_logs.csv      # Generated listening log data
├── songs_metadata.csv      # Generated song metadata
│
├── outputs/                # All task outputs are written here
│   ├── task1/              # Favorite genre per user
│   ├── task2/              # Average listen time (to be implemented)
│   ├── task3/              # Genre loyalty scores (to be implemented)
│   └── task4/              # Late-night listeners (to be implemented)
│
├── Dockerfile              # Spark-in-Docker environment (Linux)
└── docker-compose.yml      # Compose file to run Spark cleanly
```

## Output Directory Structure

```
outputs/
│
├── task1/   # CSV files for Task 1
├── task2/   # CSV files for Task 2
├── task3/   # CSV files for Task 3
└── task4/   # CSV files for Task 4
```

## Tasks and Outputs

### Task 1 — User Favorite Genres

- Join logs with metadata
- Compute total listening time per (user_id, genre)
- Rank genres per user using a window function
- Output: each user’s top genre
- Output folder: `outputs/task1/`

### Task 2 — Average Listen Time

Compute the average listening duration per user:

- Group by user_id
- Compute `avg(duration_sec)`
- Output folder: `outputs/task2/`

### Task 3 — Genre Loyalty Score

Define your own metric (example):<br>
`loyalty = total_time_for_genre / total_time_for_user`<br>
Then:

- Compute loyalty per user per genre
- Rank genres by loyalty
- Output top 10 overall
- Output folder: `outputs/task3/`

### Task 4 — Late‑Night Listeners (12 AM – 5 AM)

- Convert timestamp to hour
- Filter events where hour ∈ [0, 5]
- Output unique users who listen late at night
- Output folder: `outputs/task4/`

## Execution Instructions

### Run Spark inside Docker (recommended)

This avoids the Windows filesystem bug (`getSubject is not supported`).

#### Build the container:

`docker compose build`

#### Run the Spark job:

`docker compose run spark`

This executes:<br>
`spark-submit main.py`<br>
inside a clean Linux Spark environment.

## _Prerequisites_

Before starting the assignment, ensure you have the following software installed and properly configured on your machine:

1. _Python 3.x_:
   - [Download and Install Python](https://www.python.org/downloads/)
   - Verify installation:
     ```bash
     python3 --version
     ```

2. _PySpark_:
   - Install using pip:
     ```bash
     pip install pyspark
     ```

3. _Apache Spark_:
   - Ensure Spark is installed. You can download it from the [Apache Spark Downloads](https://spark.apache.org/downloads.html) page.
   - Verify installation by running:
     ```bash
     spark-submit --version
     ```

### _2. Running the Analysis Tasks_

#### _Running Locally_

1. _Generate the Input_:

```bash
 python3 datagen.py
```

2. **Execute Each Task Using spark-submit**:

   ```bash
     spark-submit main.py
   ```

3. _Verify the Outputs_:
   Check the outputs/ directory for the resulting files:
   ```bash
   ls outputs/
   ```

## Errors and Resolutions

- I was unable to run the `pip install pyspark` command. So, I reinstalled the latest version of Python, forced it to incluce `pip` and edited my System Environment Variables to force `Command Prompt` to use that installation.
- When installing Apache Spark, I had errors running `spark-submit main.py`, saying the command was not found. I found that I had to include the installation directory as environment variable: `%SPARK_HOME%`, and then add `%SPARK_HOME/bin` as a path.
- `python3 input_generator.py` lists the wrong file name. It should be `datagen.py`
- Running gave me a large error log. So I had to install Hadoop, and set it up similarly to Apache Spark.
- I am getting a getSubject not supported error when trying to read the CSV's. I was solved this error by creating a `Dockerfile`, `docker-compose.yml`, and running it from Docker.
